# رابط الـ Backend API عند البناء للإنتاج — انسخه إلى `.env.production`
# قبل تشغيل `npm run build`، أو ضعه في متغيرات بيئة الاستضافة.
# القيمة الافتراضية على Render (عدّلها إن اختلف رابط الخدمة الفعلية):
VITE_API_BASE_URL=https://boycott-api.onrender.com/api/v1
